import { AmadeusApiAuthGuard } from './amadeus-api-auth.guard';

describe('AmadeusApiAuthGuard', () => {
  it('should be defined', () => {
    expect(new AmadeusApiAuthGuard()).toBeDefined();
  });
});
